> clone this repo into `~/.config` and renamed it into `nvim`, and then install all dependencies in `requirements.md` file

> to delete, run:

- `rm -rf ~/.config/nvim ~/.local/share/nvim ~/.cache/nvim`
